<%= form_for(@attached, :html => {:multipart => true}) do |f| %>
    <% if @attached.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@attached.errors.count, "error") %> prohibited this attached from being saved:</h2>

          <ul>
            <% @attached.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <br/>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :img %>
      <br/>
      <%= f.file_field :img %>
    </div>

    <div class="field">
      <%= f.label :att %>
      <br/>
      <%= f.file_field :att %>
    </div>

    <input type="text" name="_format" value="json"/>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
